spring.datasource.url = jdbc:postgresql://localhost:5432/virokr
spring.datasource.username = postgres
spring.datasource.password = pg13748929
spring.datasource.driverClassName = org.postgresql.Driver

# Jpa configuration parameters
spring.jpa.generate-ddl = false
# Display SQL code execution
spring.jpa.show-sql = false 
spring.jpa.database-platform = org.hibernate.dialect.PostgreSQL82Dialect
 # Do nothing  with database on startup
spring.jpa.hibernate.ddl-auto = update

spring.jackson.serialization.fail-on-empty-beans=false

# Disable spring boot integrated security
security.basic.enabled = false

# Encoding secret key
virokr.jwtSecret = secret
virokr.jwtExpirationMs = 86400000

springdoc.swagger-ui.path=/swagger-ui.html
